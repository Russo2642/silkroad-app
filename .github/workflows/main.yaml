name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY_NAME: "silkroad-api"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Log in to DigitalOcean Docker Registry
        run: echo "${{ secrets.ACCESS_TOKEN }}" | docker login registry.digitalocean.com -u "docean" --password-stdin

      - name: Build Docker image
        run: docker build -t registry.digitalocean.com/$(echo $REGISTRY_NAME)/silkroad-app:latest .

      - name: Push Docker image to DigitalOcean Registry
        run: docker push registry.digitalocean.com/$(echo $REGISTRY_NAME)/silkroad-app:latest

    outputs:
      image_name: registry.digitalocean.com/$(echo $REGISTRY_NAME)/silkroad-app
      image_tag: latest

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Set up SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.SSHKEY }}

      - name: Deploy to DigitalOcean
        run: |
          ssh -o StrictHostKeyChecking=no root@${{ secrets.DROPLET_IP }} << 'EOF'
            sudo docker stop silkroad-app || true
            sudo docker rm silkroad-app || true
            sudo docker pull registry.digitalocean.com/$(echo $REGISTRY_NAME)/silkroad-app:latest
            sudo docker run -d --name silkroad-app -p 80:80 registry.digitalocean.com/$REGISTRY_NAME/silkroad-app:latest
          EOF